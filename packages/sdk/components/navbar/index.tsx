import { Toolbar, AppBar } from "@mui/material";

export const Navbar: React.FC = () => {
    return (
        <AppBar variant="outlined" position="static" color="transparent">
            <Toolbar variant="regular">Hello</Toolbar>
        </AppBar>
    );
};
